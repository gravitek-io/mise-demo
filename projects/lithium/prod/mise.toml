[env]
CLUSTER_NAME = "lithium-prod"
OS_REGION_NAME = "TLS31"

[hooks]
enter = """
if [ -n "$CLUSTER_NAME" ] && kubectl config get-contexts "$CLUSTER_NAME" &>/dev/null; then
  kubectl config use-context "$CLUSTER_NAME"
fi
"""
